#include <bits/stdc++.h>
using namespace std;
float dist=0,pos;
int min(float a,float b){
	if (a<b)  return a;
	else   return b;
}
float timCapDiem(float a[1000],int l,int r){
	float Min=10e9;
	if((r-l)<=3){
		if (abs(a[l+1]-a[l])-abs(a[r-1]-a[r-2])<=0)
	    { return abs(a[l+1]-a[l]);}
	    else return abs(a[r-1]-a[r-2]);
	    
	}
	int m=(l+r+1)/2;
	float dist_left=timCapDiem(a,l,m);  
	float dist_right=timCapDiem(a,m+1,r);
	float dist_min=min(dist_left,dist_right);
//	cout<<dist_left<<dist_right;
	float temp[1000];
	int index=0;
	for(int i=l;i<r;i++){
		if (abs(a[m]-a[i])<=dist_min){
			temp[index]=a[i];
			index++;
		}
	}
	for (int i=0;i<index-1;i++){
		if(Min>=(temp[i+1]-temp[i])){
		    Min=temp[i+1]-temp[i];
		}
	}
	return min(dist_min,Min);
	
}
int main() {
	int n;
	float a[1000];
	cin>>n;
	for (int i=0;i<n;i++){
	    cin>>a[i];
	   }
//	sort(a,0)
    sort(a,a+n);
    if (n==2){
    	cout<<"Khoang cach min: "<<a[1]-a[0]<<" cua 2 cap diem: "<<a[0]<<" va "<<a[1];
    }
    else{
    	dist=timCapDiem(a,0,n);
    	for (int i=0;i<n-1;i++){
	    	if(dist==a[i+1]-a[i]){
	    	    cout<<"Koang cach min: "<<dist<<" của 2 cap diem: "<<a[i]<<" va "<<a[i+1]<<endl;
	    	}
    	}
    }
    	return 0;
}